services:
  frontend:
    environment:
      COOKIE_SECURE: "true"

  nginx:
    image: nginx:1.27-alpine
    profiles: ["prod"]
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "80:80"
    depends_on:
      - frontend
      - backend
