DB_USER=bussolafin
DB_PASSWORD=change-me
JWT_SECRET=change-me-to-a-long-random-string-in-production

# Set to "true" only when the app is served over HTTPS (e.g. behind the nginx prod profile).
# Left unset/false for local docker-compose over plain HTTP.
COOKIE_SECURE=false
